import React from 'react';

class DonateComponent extends React.Component {
    render() {
        return(
        <div>
            Donations, patreon, affiliate links, etc
        </div>
        );
    }
};

export default DonateComponent;